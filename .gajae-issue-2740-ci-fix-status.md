# Issue #2740 / PR #2741 CI-fix status

Date: 2026-06-06

## Preserved dirty diff inspected

- Dirty file at recovery: `templates/AGENTS.md`.
- Preserved diff added a compact `When to use what` block under `<execution_protocols>`.
- Purpose: satisfy the failing `explore + sparkshell guidance contract` smoke test by keeping `templates/AGENTS.md` aligned with the root/generated AGENTS guidance contract:
  - mentions `USE_OMX_EXPLORE_CMD` as compatibility-only,
  - keeps `omx explore` deprecated / not preferred,
  - points normal repository lookup to Codex inspection tools/subagents,
  - keeps `omx sparkshell --tmux-pane` as explicit opt-in evidence aid.
- The change preserves the AGENTS debloat intent: it does not restore deprecated workflow walkthroughs or expand the generated/global AGENTS surface.

## Failed CI inspected

Command run:

```sh
gh pr checks 2741 --repo Yeachan-Heo/oh-my-codex --watch=false
gh api -H 'Accept: application/vnd.github+json' /repos/Yeachan-Heo/oh-my-codex/actions/jobs/79878646816/logs
```

Observed failure from `Test (Node 22 / smoke)` job `79878646816` in run `27062853018`:

```text
not ok 1 - keeps AGENTS root and template aligned on deprecated explore routing and opt-in sparkshell guidance
error: 'templates/AGENTS.md missing required pattern: /USE_OMX_EXPLORE_CMD/i'
location: dist/hooks/__tests__/explore-sparkshell-guidance-contract.test.js:11:5
```

## Fix committed and pushed

Commands run:

```sh
git status --short --branch
git diff -- templates/AGENTS.md
npm run build && node dist/scripts/run-test-files.js dist/hooks/__tests__/explore-sparkshell-guidance-contract.test.js
git add templates/AGENTS.md
git commit -m "Fix AGENTS explore guidance contract"
git push origin issue-2740-agents-debloat
git rev-parse HEAD
gh pr checks 2741 --repo Yeachan-Heo/oh-my-codex --watch=false
gh pr view 2741 --repo Yeachan-Heo/oh-my-codex --json number,state,isDraft,headRefName,headRefOid,baseRefName,url,mergeStateStatus,statusCheckRollup
```

Commit pushed:

```text
55e8b2a772bb3b346af3afa5f018afbd460649d0 Fix AGENTS explore guidance contract
```

## Bounded validation

Command run:

```sh
npm run build && node dist/scripts/run-test-files.js dist/hooks/__tests__/explore-sparkshell-guidance-contract.test.js
```

Result:

```text
✔ explore + sparkshell guidance contract (16.56069ms)
ℹ tests 4
ℹ suites 1
ℹ pass 4
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

## Current PR / CI state after push

PR:

```text
https://github.com/Yeachan-Heo/oh-my-codex/pull/2741
state: OPEN
isDraft: false
baseRefName: dev
headRefName: issue-2740-agents-debloat
headRefOid: 55e8b2a772bb3b346af3afa5f018afbd460649d0
mergeStateStatus: BLOCKED
```

Latest observed checks after push:

```text
Docs Check: SUCCESS
Detect CI lanes: SUCCESS
Draft Check: SUCCESS
PR Size Label: SUCCESS
PR Base Guidance: SKIPPED
Typecheck: PENDING
Build dist artifact: PENDING
Lint: PENDING
Rust Clippy: PENDING
Rust Format: PENDING
Rust Tests + Coverage Signal: PENDING
```

No merge, publish, or release action was performed.
